import { FigmaVsMonitoringToolsPage } from '@/components/FigmaVsMonitoringToolsPage'

// Revalidate immediately when CMS content changes
export const revalidate = 0;

export default async function PositioningPage() {
  // Use Figma design
  // CMS positioning content integration will be added in future enhancement
  return <FigmaVsMonitoringToolsPage />;
}
